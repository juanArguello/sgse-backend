package com.sgse.apirest.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AuthController {

	/*
	 * @Autowired JwtAuthentication jwtAuthentication;
	 * 
	 * @PostMapping(path = "/login", consumes = "application/json") public
	 * ResponseEntity<?> login(@Valid @RequestBody AuthCredentials authCredentials){
	 * String token = jwtAuthentication.autenticar(authCredentials); HttpHeaders
	 * httpHeaders = new HttpHeaders(); httpHeaders.setBearerAuth(token); return
	 * ResponseEntity.ok().headers(httpHeaders).build();
	 * 
	 * }
	 */
}
